<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/employee"
    android:layout_width="fill_parent"
    android:layout_height="60dp"
    android:layout_gravity="fill_vertical"
    android:orientation="horizontal" >

    <TextView
        android:id="@+id/settingsListItemName"
        android:layout_width="wrap_content"
        android:layout_height="50dp"
        android:layout_centerVertical="true"
        android:layout_marginLeft="5dp"
        android:gravity="center_vertical"
        android:text="Medium Text"
        android:textAppearance="?android:attr/textAppearanceMedium" />

    <Switch
        android:id="@+id/settingsSwitch"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBaseline="@+id/settingsListItemName"
        android:layout_alignBottom="@+id/settingsListItemName"
        android:layout_alignParentRight="true"
        android:scrollbars="@android:color/holo_green_dark"
        android:scrollbarStyle="outsideOverlay"
        android:shadowColor="@android:color/holo_red_light" />

</RelativeLayout>
<!-- android:background="@drawable/button_shape" -->
